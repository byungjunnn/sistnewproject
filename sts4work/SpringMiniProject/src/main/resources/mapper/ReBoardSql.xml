<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.data.mapper.ReBoardMapperInter">
	<select id="getTotalCount" resultType="int" parameterType="Map">
		select count(*) from remboard
		<if test="searchcolumn!=null and searchword!=null">
			where ${searchcolumn} like concat('%',#{searchword},'%')
		</if>
	</select>
	
	<insert id="insertReBoard" parameterType="ReBoardDto">
		insert into remboard(id,name,subject,photo,content,regroup,restep,relevel,writeday) 
		values(#{id},#{name},#{subject},#{photo},#{content},#{regroup},#{restep},#{relevel},now())
	</insert>
	
	<select id="getPagingList" parameterType="Map" resultType="ReBoardDto" >
		select * from remboard
		<if test="searchcolumn!=null and searchword!=null">
			where ${searchcolumn} like concat('%',#{searchword},'%')
		</if>
		order by regroup desc, restep asc limit #{start},#{perpage}
	</select>
	
	<select id="getData" parameterType="int" resultType="ReBoardDto">
		select * from remboard where num=#{num}
	</select>
	
	<update id="updateRestep" parameterType="Map">
		update remboard set restep=restep+1 where regroup=#{regroup} and restep>#{restep}
	</update>
	
	<update id="updateReadCount" parameterType="int">
		update remboard set readcount=readcount+1 where num=#{num}
	</update>
	
	<select id="getMaxNum" resultType="int">
		select ifnull(max(num),0) from remboard
	</select>
	
	<delete id="deleteReBoard" parameterType="int">
		delete from remboard where num=#{num}
	</delete>
	
	<update id="likesUpdate" parameterType="int">
		update remboard set likes=likes+1 where num=#{num}
	</update>
</mapper>